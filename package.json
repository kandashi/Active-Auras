{
  "name": "Active-Auras",
  "license": "MIT",
  "scripts": {
    "lint": "npx eslint ./src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildCompendiums": "cd pack_data && node build.mjs"
  },
  "devDependencies": {
    "eslint": "^8.34.0",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "2.3.2",
    "nedb": "^1.8.0",
    "@foundryvtt/foundryvtt-cli": "^1.0.0-rc.4"
  }
}
